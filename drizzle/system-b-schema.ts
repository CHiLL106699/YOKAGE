/**
 * 系統 B Schema — 獨立庫存表
 * 
 * 注意：大部分 System B 表格已整合至 schema.ts 中（帶 SystemB 後綴）。
 * 此檔案僅保留 schema.ts 中沒有的獨立表格。
 * 
 * 重複的表格（customerTags, crmTags, staffCommissions, gamesSystemB,
 * prizesSystemB, gameParticipationsSystemB, inventoryTransfersSystemB）
 * 已移至 schema.ts，此處不再重複定義。
 */

// 此檔案已清空，所有 System B 表格統一由 schema.ts 管理。
// 保留此檔案是為了不破壞 drizzle.config.ts 的引用。
